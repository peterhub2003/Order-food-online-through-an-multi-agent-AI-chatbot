# Local/dev configuration for the backend service
# This file is used by Docker Compose and when running locally.

POSTGRES_DB=backend_db
POSTGRES_USER=backend_user
POSTGRES_PASSWORD=backend_password
BACKEND_DATABASE_URL=postgresql+psycopg2://backend_user:backend_password@db:5432/backend_db
